# 📘 Python Learning Log — Chapter 1
*A quick intro to Python syntax, variables, and basic code structure.*

---

## 📝 1. Key Concepts

### 📌 Variable Assignment
- `=` assigns a value to a variable.  
- Variables can change to any type (string, boolean, number).  
- Python doesn’t require declaring types beforehand.

**Example:**
```python
x = 5
x = "hello"
x = True
```

### 📌 Function Calls
- Use the function name + parentheses: print().
- Arguments go inside the parentheses.
- '#' starts a comment.

**Example:**
```python

print("Hello, Python!")  # this prints text
```

#### 💻 Code Example
```python

if fish_number > 0:
    print("I don't want ANY fish!")

meat_number = 6
```

#### 🔍 Code Explanation
- Other languages use {} to mark code blocks.
- Python uses a colon : + indentation to create code blocks.
- meat_number = 6 is not indented, so it’s not part of the if-block.
- Strings can use single-quote ' ' or double-quote " ".
#### Quotation marks tip:
- "I don't want ANY fish!" ✔️
- 'I don't want ANY fish!' ❌ breaks due to the ' inside the string
